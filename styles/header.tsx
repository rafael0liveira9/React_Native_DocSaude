import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  notifyIconBox: {
    position: "relative",
  },
  notifyFloat: {
    position: "absolute",
    bottom: -15,
    left: -15,
    borderRadius: 100,
    width: 20,
    height: 20,
    zIndex: 2,
  },
});
